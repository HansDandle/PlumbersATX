import { PHONE_NUMBER, SITE_NAME, CITY } from '@/lib/constants';

export const metadata = {
  title: `Contact ${SITE_NAME} | Plumber in ${CITY}`,
  description: `Contact ${SITE_NAME} for plumbing services in ${CITY}. Call ${PHONE_NUMBER} or use our contact form.`,
};
